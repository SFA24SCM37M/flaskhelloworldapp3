steps:
# Build the container image
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/$flaskhelloworld2/$flaskhelloworld2', '.']
# Push the container image to Container Registry
- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'gcr.io/$flaskhelloworld2/$flaskhelloworld2']
# Deploy container image to Cloud Run
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: gcloud
  args: ['run', 'deploy', '$flaskhelloworld2', '--image', 'gcr.io/$flaskhelloworld2/$flaskhelloworld2', '--region', 'us-central1', '--platform', 'managed','--port','80']
images:
- gcr.io/$flaskhelloworld2/$flaskhelloworld2